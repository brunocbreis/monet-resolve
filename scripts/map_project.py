"""Map the project: timelines, bins, one timeline's tracks/items/markers. Run first in any session. See mr.projects.map_project."""
TIMELINE = "Raycast AI Update - Cut v2"
result = mr.projects.map_project(project, TIMELINE)
