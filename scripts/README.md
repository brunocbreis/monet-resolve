# Scripts

Task scripts that call into `monet_resolve`. Each has its parameters as UPPERCASE constants at the top and sets `result`. Edit the constants, then run:

    python -m monet_resolve.run scripts/<name>.py

The functions' docstrings explain each workaround.
